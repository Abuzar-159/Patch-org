<aura:component access="global">
	<aura:attribute name="oRecord" type="Object" />
    <aura:attribute name="IconName" type="string"/>
    <aura:attribute name="IconPath" type="string"/>
    <aura:registerEvent name="SelectedSerialBatchEvent" type="c:SerialBatchRecordEvent"/>
	<li role="presentation" class="slds-listbox__item" onclick="{!c.selectRecord}">
        <span id="listbox-option-unique-id-01" class="slds-media slds-listbox__option slds-listbox__option_entity slds-listbox__option_has-meta" role="option">
            <aura:if isTrue="{!or(not(empty(v.IconPath)),not(empty(v.IconName)))}">  
             <span class="slds-media__figure">
                  <span class="slds-icon_container customIcon" title="Description of icon when needed">
                      <aura:if isTrue="{!not(empty(v.IconName))}">
                          <lightning:icon iconName="{!v.IconName}" class="slds-icon slds-icon_small slds-icon-text-default customIcon" size="small" alternativeText="icon"/>
                          <aura:set attribute="else">
                              <img src="{!v.IconPath}" class="cl1"/>
                          </aura:set>
                      </aura:if>
                        <span class="slds-assistive-text">Description of icon</span>
                  </span>
              </span>    
            </aura:if>
              <span class="slds-media__body">  
                  <aura:if isTrue="{!not(empty(v.oRecord.ERP7__Serial_Number__c))}">
                      {!v.oRecord.ERP7__Serial_Number__r.Name}
                  </aura:if>
                  <aura:if isTrue="{!not(empty(v.oRecord.ERP7__Batch_Lot__c))}">
                      {!v.oRecord.ERP7__Batch_Lot__r.Name}
                  </aura:if>
              </span>
        </span>
    </li>

</aura:component>