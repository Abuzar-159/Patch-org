<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>In_Production</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
 In_Production__c,
 OR (
  ISCHANGED(Product__c),
  ISCHANGED(Quantity__c),
  ISCHANGED(Version__c),
  ISCHANGED(Routing__c)
 )
)</errorConditionFormula>
    <errorMessage>Manufacturing Order in production, cannot edit Product, Quantity, Version or Routing</errorMessage>
</ValidationRule>
