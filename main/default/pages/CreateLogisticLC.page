<apex:page sidebar="false" cache="false" showHeader="true" docType="html-5.0">
    <apex:includeScript value="/lightning/lightning.out.js" />

    <script>
    var orderId = '{!JSENCODE($currentPage.Parameters.orderId)}';
    var SOId = '{!JSENCODE($currentPage.Parameters.SOId)}';
    var POId= '{!JSENCODE($currentPage.Parameters.POId)}';
    var logisticId = '{!JSENCODE($currentPage.Parameters.LogId)}';
    !$Lightning.use("ERP7:SalesOrderToLogisticVF", function() {
        !$Lightning.createComponent(
            "ERP7:SalesOrderToLogistic",
            {                
                "orderId":orderId,
                "SOId":SOId,
                "POId":POId,
                "LogId" : logisticId
            },
            accounts,
            function(cmp) {
               
            });
        });
            
   
    </script>
    <div id="accounts"></div>
</apex:page>