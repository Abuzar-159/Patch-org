<apex:page standardController="Account" sidebar="false" >
  <apex:outputPanel rendered="{!If(Account.Barcode__c != '',true,false)}">

    <c:AQBarcodeZone id="brcd128" BrcdHeight="40" BrcdType="{!'code128'}" Value="{!Account.Barcode__c}">    
    </c:AQBarcodeZone>

    </apex:outputPanel>
    
    <apex:outputPanel rendered="{!If(Account.Barcode__c != '',false,true)}">
        Barcode field is empty
    </apex:outputPanel>
</apex:page>