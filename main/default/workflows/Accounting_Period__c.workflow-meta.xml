<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <fieldUpdates>
        <fullName>Update_Name_with_Year_and_Month</fullName>
        <field>Name</field>
        <formula>Text(YEAR( Start_Date__c ))&amp;&apos;-&apos;&amp; if(LEN(Text(MONTH(Start_Date__c )))&gt;1,Text(MONTH(Start_Date__c )),&apos;0&apos;&amp;Text(MONTH(Start_Date__c )))</formula>
        <name>Update Name with Year and Month</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Formula</operation>
        <protected>true</protected>
        <reevaluateOnChange>false</reevaluateOnChange>
    </fieldUpdates>
    <rules>
        <fullName>Accounting Period Update</fullName>
        <actions>
            <name>Update_Name_with_Year_and_Month</name>
            <type>FieldUpdate</type>
        </actions>
        <active>false</active>
        <criteriaItems>
            <field>Accounting_Period__c.CreatedById</field>
            <operation>notEqual</operation>
        </criteriaItems>
        <triggerType>onCreateOnly</triggerType>
    </rules>
</Workflow>
